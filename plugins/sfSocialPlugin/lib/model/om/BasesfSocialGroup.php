<?php


/**
 * Base class that represents a row from the 'sf_social_group' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Fri Jan  9 05:18:54 2015
 *
 * @package    propel.generator.plugins.sfSocialPlugin.lib.model.om
 */
abstract class BasesfSocialGroup extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'sfSocialGroupPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        sfSocialGroupPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the user_admin field.
     * @var        int
     */
    protected $user_admin;

    /**
     * The value for the title field.
     * @var        string
     */
    protected $title;

    /**
     * The value for the description field.
     * @var        string
     */
    protected $description;

    /**
     * The value for the visibility field.
     * Note: this column has a database default value of: 0
     * @var        int
     */
    protected $visibility;

    /**
     * The value for the photo field.
     * @var        string
     */
    protected $photo;

    /**
     * The value for the created_at field.
     * @var        string
     */
    protected $created_at;

    /**
     * The value for the updated_at field.
     * @var        string
     */
    protected $updated_at;

    /**
     * @var        sfGuardUser
     */
    protected $asfGuardUser;

    /**
     * @var        PropelObjectCollection|sfSocialGroupInvite[] Collection to store aggregation of sfSocialGroupInvite objects.
     */
    protected $collsfSocialGroupInvites;
    protected $collsfSocialGroupInvitesPartial;

    /**
     * @var        PropelObjectCollection|sfSocialGroupUser[] Collection to store aggregation of sfSocialGroupUser objects.
     */
    protected $collsfSocialGroupUsers;
    protected $collsfSocialGroupUsersPartial;

    /**
     * @var        PropelObjectCollection|GroupStatus[] Collection to store aggregation of GroupStatus objects.
     */
    protected $collGroupStatuss;
    protected $collGroupStatussPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $sfSocialGroupInvitesScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $sfSocialGroupUsersScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $groupStatussScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->visibility = 0;
    }

    /**
     * Initializes internal state of BasesfSocialGroup object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [user_admin] column value.
     *
     * @return int
     */
    public function getUserAdmin()
    {
        return $this->user_admin;
    }

    /**
     * Get the [title] column value.
     *
     * @return string
     */
    public function getTitle()
    {
        return $this->title;
    }

    /**
     * Get the [description] column value.
     *
     * @return string
     */
    public function getDescription()
    {
        return $this->description;
    }

    /**
     * Get the [visibility] column value.
     *
     * @return int
     */
    public function getVisibility()
    {
        return $this->visibility;
    }

    /**
     * Get the [photo] column value.
     *
     * @return string
     */
    public function getPhoto()
    {
        return $this->photo;
    }

    /**
     * Get the [optionally formatted] temporal [created_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getCreatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->created_at === null) {
            return null;
        }

        if ($this->created_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->created_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [optionally formatted] temporal [updated_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getUpdatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->updated_at === null) {
            return null;
        }

        if ($this->updated_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->updated_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = sfSocialGroupPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [user_admin] column.
     *
     * @param int $v new value
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setUserAdmin($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->user_admin !== $v) {
            $this->user_admin = $v;
            $this->modifiedColumns[] = sfSocialGroupPeer::USER_ADMIN;
        }

        if ($this->asfGuardUser !== null && $this->asfGuardUser->getId() !== $v) {
            $this->asfGuardUser = null;
        }


        return $this;
    } // setUserAdmin()

    /**
     * Set the value of [title] column.
     *
     * @param string $v new value
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setTitle($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->title !== $v) {
            $this->title = $v;
            $this->modifiedColumns[] = sfSocialGroupPeer::TITLE;
        }


        return $this;
    } // setTitle()

    /**
     * Set the value of [description] column.
     *
     * @param string $v new value
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setDescription($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->description !== $v) {
            $this->description = $v;
            $this->modifiedColumns[] = sfSocialGroupPeer::DESCRIPTION;
        }


        return $this;
    } // setDescription()

    /**
     * Set the value of [visibility] column.
     *
     * @param int $v new value
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setVisibility($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->visibility !== $v) {
            $this->visibility = $v;
            $this->modifiedColumns[] = sfSocialGroupPeer::VISIBILITY;
        }


        return $this;
    } // setVisibility()

    /**
     * Set the value of [photo] column.
     *
     * @param string $v new value
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setPhoto($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->photo !== $v) {
            $this->photo = $v;
            $this->modifiedColumns[] = sfSocialGroupPeer::PHOTO;
        }


        return $this;
    } // setPhoto()

    /**
     * Sets the value of [created_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setCreatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->created_at !== null || $dt !== null) {
            $currentDateAsString = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->created_at = $newDateAsString;
                $this->modifiedColumns[] = sfSocialGroupPeer::CREATED_AT;
            }
        } // if either are not null


        return $this;
    } // setCreatedAt()

    /**
     * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function setUpdatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->updated_at !== null || $dt !== null) {
            $currentDateAsString = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->updated_at = $newDateAsString;
                $this->modifiedColumns[] = sfSocialGroupPeer::UPDATED_AT;
            }
        } // if either are not null


        return $this;
    } // setUpdatedAt()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->visibility !== 0) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->user_admin = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->title = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->description = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->visibility = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->photo = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->created_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->updated_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 8; // 8 = sfSocialGroupPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating sfSocialGroup object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->asfGuardUser !== null && $this->user_admin !== $this->asfGuardUser->getId()) {
            $this->asfGuardUser = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(sfSocialGroupPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = sfSocialGroupPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->asfGuardUser = null;
            $this->collsfSocialGroupInvites = null;

            $this->collsfSocialGroupUsers = null;

            $this->collGroupStatuss = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(sfSocialGroupPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = sfSocialGroupQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasesfSocialGroup:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasesfSocialGroup:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(sfSocialGroupPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasesfSocialGroup:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            // symfony_timestampable behavior
            if ($this->isModified() && !$this->isColumnModified(sfSocialGroupPeer::UPDATED_AT))
            {
                $this->setUpdatedAt(time());
            }
            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
                // symfony_timestampable behavior
                if (!$this->isColumnModified(sfSocialGroupPeer::CREATED_AT))
                {
                  $this->setCreatedAt(time());
                }

            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasesfSocialGroup:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                sfSocialGroupPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->asfGuardUser !== null) {
                if ($this->asfGuardUser->isModified() || $this->asfGuardUser->isNew()) {
                    $affectedRows += $this->asfGuardUser->save($con);
                }
                $this->setsfGuardUser($this->asfGuardUser);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->sfSocialGroupInvitesScheduledForDeletion !== null) {
                if (!$this->sfSocialGroupInvitesScheduledForDeletion->isEmpty()) {
                    sfSocialGroupInviteQuery::create()
                        ->filterByPrimaryKeys($this->sfSocialGroupInvitesScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->sfSocialGroupInvitesScheduledForDeletion = null;
                }
            }

            if ($this->collsfSocialGroupInvites !== null) {
                foreach ($this->collsfSocialGroupInvites as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->sfSocialGroupUsersScheduledForDeletion !== null) {
                if (!$this->sfSocialGroupUsersScheduledForDeletion->isEmpty()) {
                    sfSocialGroupUserQuery::create()
                        ->filterByPrimaryKeys($this->sfSocialGroupUsersScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->sfSocialGroupUsersScheduledForDeletion = null;
                }
            }

            if ($this->collsfSocialGroupUsers !== null) {
                foreach ($this->collsfSocialGroupUsers as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->groupStatussScheduledForDeletion !== null) {
                if (!$this->groupStatussScheduledForDeletion->isEmpty()) {
                    GroupStatusQuery::create()
                        ->filterByPrimaryKeys($this->groupStatussScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->groupStatussScheduledForDeletion = null;
                }
            }

            if ($this->collGroupStatuss !== null) {
                foreach ($this->collGroupStatuss as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = sfSocialGroupPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . sfSocialGroupPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(sfSocialGroupPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(sfSocialGroupPeer::USER_ADMIN)) {
            $modifiedColumns[':p' . $index++]  = '`USER_ADMIN`';
        }
        if ($this->isColumnModified(sfSocialGroupPeer::TITLE)) {
            $modifiedColumns[':p' . $index++]  = '`TITLE`';
        }
        if ($this->isColumnModified(sfSocialGroupPeer::DESCRIPTION)) {
            $modifiedColumns[':p' . $index++]  = '`DESCRIPTION`';
        }
        if ($this->isColumnModified(sfSocialGroupPeer::VISIBILITY)) {
            $modifiedColumns[':p' . $index++]  = '`VISIBILITY`';
        }
        if ($this->isColumnModified(sfSocialGroupPeer::PHOTO)) {
            $modifiedColumns[':p' . $index++]  = '`PHOTO`';
        }
        if ($this->isColumnModified(sfSocialGroupPeer::CREATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`CREATED_AT`';
        }
        if ($this->isColumnModified(sfSocialGroupPeer::UPDATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`UPDATED_AT`';
        }

        $sql = sprintf(
            'INSERT INTO `sf_social_group` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`USER_ADMIN`':
                        $stmt->bindValue($identifier, $this->user_admin, PDO::PARAM_INT);
                        break;
                    case '`TITLE`':
                        $stmt->bindValue($identifier, $this->title, PDO::PARAM_STR);
                        break;
                    case '`DESCRIPTION`':
                        $stmt->bindValue($identifier, $this->description, PDO::PARAM_STR);
                        break;
                    case '`VISIBILITY`':
                        $stmt->bindValue($identifier, $this->visibility, PDO::PARAM_INT);
                        break;
                    case '`PHOTO`':
                        $stmt->bindValue($identifier, $this->photo, PDO::PARAM_STR);
                        break;
                    case '`CREATED_AT`':
                        $stmt->bindValue($identifier, $this->created_at, PDO::PARAM_STR);
                        break;
                    case '`UPDATED_AT`':
                        $stmt->bindValue($identifier, $this->updated_at, PDO::PARAM_STR);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->asfGuardUser !== null) {
                if (!$this->asfGuardUser->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->asfGuardUser->getValidationFailures());
                }
            }


            if (($retval = sfSocialGroupPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collsfSocialGroupInvites !== null) {
                    foreach ($this->collsfSocialGroupInvites as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collsfSocialGroupUsers !== null) {
                    foreach ($this->collsfSocialGroupUsers as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collGroupStatuss !== null) {
                    foreach ($this->collGroupStatuss as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = sfSocialGroupPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getUserAdmin();
                break;
            case 2:
                return $this->getTitle();
                break;
            case 3:
                return $this->getDescription();
                break;
            case 4:
                return $this->getVisibility();
                break;
            case 5:
                return $this->getPhoto();
                break;
            case 6:
                return $this->getCreatedAt();
                break;
            case 7:
                return $this->getUpdatedAt();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['sfSocialGroup'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['sfSocialGroup'][$this->getPrimaryKey()] = true;
        $keys = sfSocialGroupPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getUserAdmin(),
            $keys[2] => $this->getTitle(),
            $keys[3] => $this->getDescription(),
            $keys[4] => $this->getVisibility(),
            $keys[5] => $this->getPhoto(),
            $keys[6] => $this->getCreatedAt(),
            $keys[7] => $this->getUpdatedAt(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->asfGuardUser) {
                $result['sfGuardUser'] = $this->asfGuardUser->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collsfSocialGroupInvites) {
                $result['sfSocialGroupInvites'] = $this->collsfSocialGroupInvites->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collsfSocialGroupUsers) {
                $result['sfSocialGroupUsers'] = $this->collsfSocialGroupUsers->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collGroupStatuss) {
                $result['GroupStatuss'] = $this->collGroupStatuss->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = sfSocialGroupPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setUserAdmin($value);
                break;
            case 2:
                $this->setTitle($value);
                break;
            case 3:
                $this->setDescription($value);
                break;
            case 4:
                $this->setVisibility($value);
                break;
            case 5:
                $this->setPhoto($value);
                break;
            case 6:
                $this->setCreatedAt($value);
                break;
            case 7:
                $this->setUpdatedAt($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = sfSocialGroupPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setUserAdmin($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setTitle($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setDescription($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setVisibility($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setPhoto($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setCreatedAt($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setUpdatedAt($arr[$keys[7]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(sfSocialGroupPeer::DATABASE_NAME);

        if ($this->isColumnModified(sfSocialGroupPeer::ID)) $criteria->add(sfSocialGroupPeer::ID, $this->id);
        if ($this->isColumnModified(sfSocialGroupPeer::USER_ADMIN)) $criteria->add(sfSocialGroupPeer::USER_ADMIN, $this->user_admin);
        if ($this->isColumnModified(sfSocialGroupPeer::TITLE)) $criteria->add(sfSocialGroupPeer::TITLE, $this->title);
        if ($this->isColumnModified(sfSocialGroupPeer::DESCRIPTION)) $criteria->add(sfSocialGroupPeer::DESCRIPTION, $this->description);
        if ($this->isColumnModified(sfSocialGroupPeer::VISIBILITY)) $criteria->add(sfSocialGroupPeer::VISIBILITY, $this->visibility);
        if ($this->isColumnModified(sfSocialGroupPeer::PHOTO)) $criteria->add(sfSocialGroupPeer::PHOTO, $this->photo);
        if ($this->isColumnModified(sfSocialGroupPeer::CREATED_AT)) $criteria->add(sfSocialGroupPeer::CREATED_AT, $this->created_at);
        if ($this->isColumnModified(sfSocialGroupPeer::UPDATED_AT)) $criteria->add(sfSocialGroupPeer::UPDATED_AT, $this->updated_at);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(sfSocialGroupPeer::DATABASE_NAME);
        $criteria->add(sfSocialGroupPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of sfSocialGroup (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setUserAdmin($this->getUserAdmin());
        $copyObj->setTitle($this->getTitle());
        $copyObj->setDescription($this->getDescription());
        $copyObj->setVisibility($this->getVisibility());
        $copyObj->setPhoto($this->getPhoto());
        $copyObj->setCreatedAt($this->getCreatedAt());
        $copyObj->setUpdatedAt($this->getUpdatedAt());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getsfSocialGroupInvites() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addsfSocialGroupInvite($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getsfSocialGroupUsers() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addsfSocialGroupUser($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getGroupStatuss() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addGroupStatus($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return sfSocialGroup Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return sfSocialGroupPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new sfSocialGroupPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a sfGuardUser object.
     *
     * @param             sfGuardUser $v
     * @return sfSocialGroup The current object (for fluent API support)
     * @throws PropelException
     */
    public function setsfGuardUser(sfGuardUser $v = null)
    {
        if ($v === null) {
            $this->setUserAdmin(NULL);
        } else {
            $this->setUserAdmin($v->getId());
        }

        $this->asfGuardUser = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the sfGuardUser object, it will not be re-added.
        if ($v !== null) {
            $v->addsfSocialGroup($this);
        }


        return $this;
    }


    /**
     * Get the associated sfGuardUser object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return sfGuardUser The associated sfGuardUser object.
     * @throws PropelException
     */
    public function getsfGuardUser(PropelPDO $con = null)
    {
        if ($this->asfGuardUser === null && ($this->user_admin !== null)) {
            $this->asfGuardUser = sfGuardUserQuery::create()->findPk($this->user_admin, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->asfGuardUser->addsfSocialGroups($this);
             */
        }

        return $this->asfGuardUser;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('sfSocialGroupInvite' == $relationName) {
            $this->initsfSocialGroupInvites();
        }
        if ('sfSocialGroupUser' == $relationName) {
            $this->initsfSocialGroupUsers();
        }
        if ('GroupStatus' == $relationName) {
            $this->initGroupStatuss();
        }
    }

    /**
     * Clears out the collsfSocialGroupInvites collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addsfSocialGroupInvites()
     */
    public function clearsfSocialGroupInvites()
    {
        $this->collsfSocialGroupInvites = null; // important to set this to null since that means it is uninitialized
        $this->collsfSocialGroupInvitesPartial = null;
    }

    /**
     * reset is the collsfSocialGroupInvites collection loaded partially
     *
     * @return void
     */
    public function resetPartialsfSocialGroupInvites($v = true)
    {
        $this->collsfSocialGroupInvitesPartial = $v;
    }

    /**
     * Initializes the collsfSocialGroupInvites collection.
     *
     * By default this just sets the collsfSocialGroupInvites collection to an empty array (like clearcollsfSocialGroupInvites());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initsfSocialGroupInvites($overrideExisting = true)
    {
        if (null !== $this->collsfSocialGroupInvites && !$overrideExisting) {
            return;
        }
        $this->collsfSocialGroupInvites = new PropelObjectCollection();
        $this->collsfSocialGroupInvites->setModel('sfSocialGroupInvite');
    }

    /**
     * Gets an array of sfSocialGroupInvite objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this sfSocialGroup is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|sfSocialGroupInvite[] List of sfSocialGroupInvite objects
     * @throws PropelException
     */
    public function getsfSocialGroupInvites($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialGroupInvitesPartial && !$this->isNew();
        if (null === $this->collsfSocialGroupInvites || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collsfSocialGroupInvites) {
                // return empty collection
                $this->initsfSocialGroupInvites();
            } else {
                $collsfSocialGroupInvites = sfSocialGroupInviteQuery::create(null, $criteria)
                    ->filterBysfSocialGroup($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collsfSocialGroupInvitesPartial && count($collsfSocialGroupInvites)) {
                      $this->initsfSocialGroupInvites(false);

                      foreach($collsfSocialGroupInvites as $obj) {
                        if (false == $this->collsfSocialGroupInvites->contains($obj)) {
                          $this->collsfSocialGroupInvites->append($obj);
                        }
                      }

                      $this->collsfSocialGroupInvitesPartial = true;
                    }

                    return $collsfSocialGroupInvites;
                }

                if($partial && $this->collsfSocialGroupInvites) {
                    foreach($this->collsfSocialGroupInvites as $obj) {
                        if($obj->isNew()) {
                            $collsfSocialGroupInvites[] = $obj;
                        }
                    }
                }

                $this->collsfSocialGroupInvites = $collsfSocialGroupInvites;
                $this->collsfSocialGroupInvitesPartial = false;
            }
        }

        return $this->collsfSocialGroupInvites;
    }

    /**
     * Sets a collection of sfSocialGroupInvite objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $sfSocialGroupInvites A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setsfSocialGroupInvites(PropelCollection $sfSocialGroupInvites, PropelPDO $con = null)
    {
        $this->sfSocialGroupInvitesScheduledForDeletion = $this->getsfSocialGroupInvites(new Criteria(), $con)->diff($sfSocialGroupInvites);

        foreach ($this->sfSocialGroupInvitesScheduledForDeletion as $sfSocialGroupInviteRemoved) {
            $sfSocialGroupInviteRemoved->setsfSocialGroup(null);
        }

        $this->collsfSocialGroupInvites = null;
        foreach ($sfSocialGroupInvites as $sfSocialGroupInvite) {
            $this->addsfSocialGroupInvite($sfSocialGroupInvite);
        }

        $this->collsfSocialGroupInvites = $sfSocialGroupInvites;
        $this->collsfSocialGroupInvitesPartial = false;
    }

    /**
     * Returns the number of related sfSocialGroupInvite objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related sfSocialGroupInvite objects.
     * @throws PropelException
     */
    public function countsfSocialGroupInvites(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialGroupInvitesPartial && !$this->isNew();
        if (null === $this->collsfSocialGroupInvites || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collsfSocialGroupInvites) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getsfSocialGroupInvites());
                }
                $query = sfSocialGroupInviteQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBysfSocialGroup($this)
                    ->count($con);
            }
        } else {
            return count($this->collsfSocialGroupInvites);
        }
    }

    /**
     * Method called to associate a sfSocialGroupInvite object to this object
     * through the sfSocialGroupInvite foreign key attribute.
     *
     * @param    sfSocialGroupInvite $l sfSocialGroupInvite
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function addsfSocialGroupInvite(sfSocialGroupInvite $l)
    {
        if ($this->collsfSocialGroupInvites === null) {
            $this->initsfSocialGroupInvites();
            $this->collsfSocialGroupInvitesPartial = true;
        }
        if (!$this->collsfSocialGroupInvites->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddsfSocialGroupInvite($l);
        }

        return $this;
    }

    /**
     * @param	sfSocialGroupInvite $sfSocialGroupInvite The sfSocialGroupInvite object to add.
     */
    protected function doAddsfSocialGroupInvite($sfSocialGroupInvite)
    {
        $this->collsfSocialGroupInvites[]= $sfSocialGroupInvite;
        $sfSocialGroupInvite->setsfSocialGroup($this);
    }

    /**
     * @param	sfSocialGroupInvite $sfSocialGroupInvite The sfSocialGroupInvite object to remove.
     */
    public function removesfSocialGroupInvite($sfSocialGroupInvite)
    {
        if ($this->getsfSocialGroupInvites()->contains($sfSocialGroupInvite)) {
            $this->collsfSocialGroupInvites->remove($this->collsfSocialGroupInvites->search($sfSocialGroupInvite));
            if (null === $this->sfSocialGroupInvitesScheduledForDeletion) {
                $this->sfSocialGroupInvitesScheduledForDeletion = clone $this->collsfSocialGroupInvites;
                $this->sfSocialGroupInvitesScheduledForDeletion->clear();
            }
            $this->sfSocialGroupInvitesScheduledForDeletion[]= $sfSocialGroupInvite;
            $sfSocialGroupInvite->setsfSocialGroup(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this sfSocialGroup is new, it will return
     * an empty collection; or if this sfSocialGroup has previously
     * been saved, it will retrieve related sfSocialGroupInvites from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in sfSocialGroup.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|sfSocialGroupInvite[] List of sfSocialGroupInvite objects
     */
    public function getsfSocialGroupInvitesJoinsfGuardUserRelatedByUserId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = sfSocialGroupInviteQuery::create(null, $criteria);
        $query->joinWith('sfGuardUserRelatedByUserId', $join_behavior);

        return $this->getsfSocialGroupInvites($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this sfSocialGroup is new, it will return
     * an empty collection; or if this sfSocialGroup has previously
     * been saved, it will retrieve related sfSocialGroupInvites from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in sfSocialGroup.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|sfSocialGroupInvite[] List of sfSocialGroupInvite objects
     */
    public function getsfSocialGroupInvitesJoinsfGuardUserRelatedByUserFrom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = sfSocialGroupInviteQuery::create(null, $criteria);
        $query->joinWith('sfGuardUserRelatedByUserFrom', $join_behavior);

        return $this->getsfSocialGroupInvites($query, $con);
    }

    /**
     * Clears out the collsfSocialGroupUsers collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addsfSocialGroupUsers()
     */
    public function clearsfSocialGroupUsers()
    {
        $this->collsfSocialGroupUsers = null; // important to set this to null since that means it is uninitialized
        $this->collsfSocialGroupUsersPartial = null;
    }

    /**
     * reset is the collsfSocialGroupUsers collection loaded partially
     *
     * @return void
     */
    public function resetPartialsfSocialGroupUsers($v = true)
    {
        $this->collsfSocialGroupUsersPartial = $v;
    }

    /**
     * Initializes the collsfSocialGroupUsers collection.
     *
     * By default this just sets the collsfSocialGroupUsers collection to an empty array (like clearcollsfSocialGroupUsers());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initsfSocialGroupUsers($overrideExisting = true)
    {
        if (null !== $this->collsfSocialGroupUsers && !$overrideExisting) {
            return;
        }
        $this->collsfSocialGroupUsers = new PropelObjectCollection();
        $this->collsfSocialGroupUsers->setModel('sfSocialGroupUser');
    }

    /**
     * Gets an array of sfSocialGroupUser objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this sfSocialGroup is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|sfSocialGroupUser[] List of sfSocialGroupUser objects
     * @throws PropelException
     */
    public function getsfSocialGroupUsers($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialGroupUsersPartial && !$this->isNew();
        if (null === $this->collsfSocialGroupUsers || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collsfSocialGroupUsers) {
                // return empty collection
                $this->initsfSocialGroupUsers();
            } else {
                $collsfSocialGroupUsers = sfSocialGroupUserQuery::create(null, $criteria)
                    ->filterBysfSocialGroup($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collsfSocialGroupUsersPartial && count($collsfSocialGroupUsers)) {
                      $this->initsfSocialGroupUsers(false);

                      foreach($collsfSocialGroupUsers as $obj) {
                        if (false == $this->collsfSocialGroupUsers->contains($obj)) {
                          $this->collsfSocialGroupUsers->append($obj);
                        }
                      }

                      $this->collsfSocialGroupUsersPartial = true;
                    }

                    return $collsfSocialGroupUsers;
                }

                if($partial && $this->collsfSocialGroupUsers) {
                    foreach($this->collsfSocialGroupUsers as $obj) {
                        if($obj->isNew()) {
                            $collsfSocialGroupUsers[] = $obj;
                        }
                    }
                }

                $this->collsfSocialGroupUsers = $collsfSocialGroupUsers;
                $this->collsfSocialGroupUsersPartial = false;
            }
        }

        return $this->collsfSocialGroupUsers;
    }

    /**
     * Sets a collection of sfSocialGroupUser objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $sfSocialGroupUsers A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setsfSocialGroupUsers(PropelCollection $sfSocialGroupUsers, PropelPDO $con = null)
    {
        $this->sfSocialGroupUsersScheduledForDeletion = $this->getsfSocialGroupUsers(new Criteria(), $con)->diff($sfSocialGroupUsers);

        foreach ($this->sfSocialGroupUsersScheduledForDeletion as $sfSocialGroupUserRemoved) {
            $sfSocialGroupUserRemoved->setsfSocialGroup(null);
        }

        $this->collsfSocialGroupUsers = null;
        foreach ($sfSocialGroupUsers as $sfSocialGroupUser) {
            $this->addsfSocialGroupUser($sfSocialGroupUser);
        }

        $this->collsfSocialGroupUsers = $sfSocialGroupUsers;
        $this->collsfSocialGroupUsersPartial = false;
    }

    /**
     * Returns the number of related sfSocialGroupUser objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related sfSocialGroupUser objects.
     * @throws PropelException
     */
    public function countsfSocialGroupUsers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialGroupUsersPartial && !$this->isNew();
        if (null === $this->collsfSocialGroupUsers || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collsfSocialGroupUsers) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getsfSocialGroupUsers());
                }
                $query = sfSocialGroupUserQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBysfSocialGroup($this)
                    ->count($con);
            }
        } else {
            return count($this->collsfSocialGroupUsers);
        }
    }

    /**
     * Method called to associate a sfSocialGroupUser object to this object
     * through the sfSocialGroupUser foreign key attribute.
     *
     * @param    sfSocialGroupUser $l sfSocialGroupUser
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function addsfSocialGroupUser(sfSocialGroupUser $l)
    {
        if ($this->collsfSocialGroupUsers === null) {
            $this->initsfSocialGroupUsers();
            $this->collsfSocialGroupUsersPartial = true;
        }
        if (!$this->collsfSocialGroupUsers->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddsfSocialGroupUser($l);
        }

        return $this;
    }

    /**
     * @param	sfSocialGroupUser $sfSocialGroupUser The sfSocialGroupUser object to add.
     */
    protected function doAddsfSocialGroupUser($sfSocialGroupUser)
    {
        $this->collsfSocialGroupUsers[]= $sfSocialGroupUser;
        $sfSocialGroupUser->setsfSocialGroup($this);
    }

    /**
     * @param	sfSocialGroupUser $sfSocialGroupUser The sfSocialGroupUser object to remove.
     */
    public function removesfSocialGroupUser($sfSocialGroupUser)
    {
        if ($this->getsfSocialGroupUsers()->contains($sfSocialGroupUser)) {
            $this->collsfSocialGroupUsers->remove($this->collsfSocialGroupUsers->search($sfSocialGroupUser));
            if (null === $this->sfSocialGroupUsersScheduledForDeletion) {
                $this->sfSocialGroupUsersScheduledForDeletion = clone $this->collsfSocialGroupUsers;
                $this->sfSocialGroupUsersScheduledForDeletion->clear();
            }
            $this->sfSocialGroupUsersScheduledForDeletion[]= $sfSocialGroupUser;
            $sfSocialGroupUser->setsfSocialGroup(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this sfSocialGroup is new, it will return
     * an empty collection; or if this sfSocialGroup has previously
     * been saved, it will retrieve related sfSocialGroupUsers from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in sfSocialGroup.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|sfSocialGroupUser[] List of sfSocialGroupUser objects
     */
    public function getsfSocialGroupUsersJoinsfGuardUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = sfSocialGroupUserQuery::create(null, $criteria);
        $query->joinWith('sfGuardUser', $join_behavior);

        return $this->getsfSocialGroupUsers($query, $con);
    }

    /**
     * Clears out the collGroupStatuss collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addGroupStatuss()
     */
    public function clearGroupStatuss()
    {
        $this->collGroupStatuss = null; // important to set this to null since that means it is uninitialized
        $this->collGroupStatussPartial = null;
    }

    /**
     * reset is the collGroupStatuss collection loaded partially
     *
     * @return void
     */
    public function resetPartialGroupStatuss($v = true)
    {
        $this->collGroupStatussPartial = $v;
    }

    /**
     * Initializes the collGroupStatuss collection.
     *
     * By default this just sets the collGroupStatuss collection to an empty array (like clearcollGroupStatuss());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initGroupStatuss($overrideExisting = true)
    {
        if (null !== $this->collGroupStatuss && !$overrideExisting) {
            return;
        }
        $this->collGroupStatuss = new PropelObjectCollection();
        $this->collGroupStatuss->setModel('GroupStatus');
    }

    /**
     * Gets an array of GroupStatus objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this sfSocialGroup is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|GroupStatus[] List of GroupStatus objects
     * @throws PropelException
     */
    public function getGroupStatuss($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collGroupStatussPartial && !$this->isNew();
        if (null === $this->collGroupStatuss || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collGroupStatuss) {
                // return empty collection
                $this->initGroupStatuss();
            } else {
                $collGroupStatuss = GroupStatusQuery::create(null, $criteria)
                    ->filterBysfSocialGroup($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collGroupStatussPartial && count($collGroupStatuss)) {
                      $this->initGroupStatuss(false);

                      foreach($collGroupStatuss as $obj) {
                        if (false == $this->collGroupStatuss->contains($obj)) {
                          $this->collGroupStatuss->append($obj);
                        }
                      }

                      $this->collGroupStatussPartial = true;
                    }

                    return $collGroupStatuss;
                }

                if($partial && $this->collGroupStatuss) {
                    foreach($this->collGroupStatuss as $obj) {
                        if($obj->isNew()) {
                            $collGroupStatuss[] = $obj;
                        }
                    }
                }

                $this->collGroupStatuss = $collGroupStatuss;
                $this->collGroupStatussPartial = false;
            }
        }

        return $this->collGroupStatuss;
    }

    /**
     * Sets a collection of GroupStatus objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $groupStatuss A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setGroupStatuss(PropelCollection $groupStatuss, PropelPDO $con = null)
    {
        $this->groupStatussScheduledForDeletion = $this->getGroupStatuss(new Criteria(), $con)->diff($groupStatuss);

        foreach ($this->groupStatussScheduledForDeletion as $groupStatusRemoved) {
            $groupStatusRemoved->setsfSocialGroup(null);
        }

        $this->collGroupStatuss = null;
        foreach ($groupStatuss as $groupStatus) {
            $this->addGroupStatus($groupStatus);
        }

        $this->collGroupStatuss = $groupStatuss;
        $this->collGroupStatussPartial = false;
    }

    /**
     * Returns the number of related GroupStatus objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related GroupStatus objects.
     * @throws PropelException
     */
    public function countGroupStatuss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collGroupStatussPartial && !$this->isNew();
        if (null === $this->collGroupStatuss || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collGroupStatuss) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getGroupStatuss());
                }
                $query = GroupStatusQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBysfSocialGroup($this)
                    ->count($con);
            }
        } else {
            return count($this->collGroupStatuss);
        }
    }

    /**
     * Method called to associate a GroupStatus object to this object
     * through the GroupStatus foreign key attribute.
     *
     * @param    GroupStatus $l GroupStatus
     * @return sfSocialGroup The current object (for fluent API support)
     */
    public function addGroupStatus(GroupStatus $l)
    {
        if ($this->collGroupStatuss === null) {
            $this->initGroupStatuss();
            $this->collGroupStatussPartial = true;
        }
        if (!$this->collGroupStatuss->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddGroupStatus($l);
        }

        return $this;
    }

    /**
     * @param	GroupStatus $groupStatus The groupStatus object to add.
     */
    protected function doAddGroupStatus($groupStatus)
    {
        $this->collGroupStatuss[]= $groupStatus;
        $groupStatus->setsfSocialGroup($this);
    }

    /**
     * @param	GroupStatus $groupStatus The groupStatus object to remove.
     */
    public function removeGroupStatus($groupStatus)
    {
        if ($this->getGroupStatuss()->contains($groupStatus)) {
            $this->collGroupStatuss->remove($this->collGroupStatuss->search($groupStatus));
            if (null === $this->groupStatussScheduledForDeletion) {
                $this->groupStatussScheduledForDeletion = clone $this->collGroupStatuss;
                $this->groupStatussScheduledForDeletion->clear();
            }
            $this->groupStatussScheduledForDeletion[]= $groupStatus;
            $groupStatus->setsfSocialGroup(null);
        }
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->user_admin = null;
        $this->title = null;
        $this->description = null;
        $this->visibility = null;
        $this->photo = null;
        $this->created_at = null;
        $this->updated_at = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collsfSocialGroupInvites) {
                foreach ($this->collsfSocialGroupInvites as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collsfSocialGroupUsers) {
                foreach ($this->collsfSocialGroupUsers as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collGroupStatuss) {
                foreach ($this->collGroupStatuss as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collsfSocialGroupInvites instanceof PropelCollection) {
            $this->collsfSocialGroupInvites->clearIterator();
        }
        $this->collsfSocialGroupInvites = null;
        if ($this->collsfSocialGroupUsers instanceof PropelCollection) {
            $this->collsfSocialGroupUsers->clearIterator();
        }
        $this->collsfSocialGroupUsers = null;
        if ($this->collGroupStatuss instanceof PropelCollection) {
            $this->collGroupStatuss->clearIterator();
        }
        $this->collGroupStatuss = null;
        $this->asfGuardUser = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(sfSocialGroupPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BasesfSocialGroup:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
