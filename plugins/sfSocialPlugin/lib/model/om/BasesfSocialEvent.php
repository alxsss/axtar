<?php


/**
 * Base class that represents a row from the 'sf_social_event' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Fri Jan  9 03:26:42 2015
 *
 * @package    propel.generator.plugins.sfSocialPlugin.lib.model.om
 */
abstract class BasesfSocialEvent extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'sfSocialEventPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        sfSocialEventPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the user_admin field.
     * @var        int
     */
    protected $user_admin;

    /**
     * The value for the title field.
     * @var        string
     */
    protected $title;

    /**
     * The value for the description field.
     * @var        string
     */
    protected $description;

    /**
     * The value for the visibility field.
     * Note: this column has a database default value of: 0
     * @var        int
     */
    protected $visibility;

    /**
     * The value for the photo field.
     * @var        string
     */
    protected $photo;

    /**
     * The value for the start field.
     * @var        string
     */
    protected $start;

    /**
     * The value for the end field.
     * @var        string
     */
    protected $end;

    /**
     * The value for the location field.
     * @var        string
     */
    protected $location;

    /**
     * The value for the created_at field.
     * @var        string
     */
    protected $created_at;

    /**
     * The value for the updated_at field.
     * @var        string
     */
    protected $updated_at;

    /**
     * @var        sfGuardUser
     */
    protected $asfGuardUser;

    /**
     * @var        PropelObjectCollection|sfSocialEventInvite[] Collection to store aggregation of sfSocialEventInvite objects.
     */
    protected $collsfSocialEventInvites;
    protected $collsfSocialEventInvitesPartial;

    /**
     * @var        PropelObjectCollection|sfSocialEventUser[] Collection to store aggregation of sfSocialEventUser objects.
     */
    protected $collsfSocialEventUsers;
    protected $collsfSocialEventUsersPartial;

    /**
     * @var        PropelObjectCollection|EventStatus[] Collection to store aggregation of EventStatus objects.
     */
    protected $collEventStatuss;
    protected $collEventStatussPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $sfSocialEventInvitesScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $sfSocialEventUsersScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $eventStatussScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->visibility = 0;
    }

    /**
     * Initializes internal state of BasesfSocialEvent object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [user_admin] column value.
     *
     * @return int
     */
    public function getUserAdmin()
    {
        return $this->user_admin;
    }

    /**
     * Get the [title] column value.
     *
     * @return string
     */
    public function getTitle()
    {
        return $this->title;
    }

    /**
     * Get the [description] column value.
     *
     * @return string
     */
    public function getDescription()
    {
        return $this->description;
    }

    /**
     * Get the [visibility] column value.
     *
     * @return int
     */
    public function getVisibility()
    {
        return $this->visibility;
    }

    /**
     * Get the [photo] column value.
     *
     * @return string
     */
    public function getPhoto()
    {
        return $this->photo;
    }

    /**
     * Get the [optionally formatted] temporal [start] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getStart($format = 'Y-m-d H:i:s')
    {
        if ($this->start === null) {
            return null;
        }

        if ($this->start === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->start);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->start, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [optionally formatted] temporal [end] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getEnd($format = 'Y-m-d H:i:s')
    {
        if ($this->end === null) {
            return null;
        }

        if ($this->end === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->end);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->end, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [location] column value.
     *
     * @return string
     */
    public function getLocation()
    {
        return $this->location;
    }

    /**
     * Get the [optionally formatted] temporal [created_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getCreatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->created_at === null) {
            return null;
        }

        if ($this->created_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->created_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [optionally formatted] temporal [updated_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getUpdatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->updated_at === null) {
            return null;
        }

        if ($this->updated_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->updated_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = sfSocialEventPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [user_admin] column.
     *
     * @param int $v new value
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setUserAdmin($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->user_admin !== $v) {
            $this->user_admin = $v;
            $this->modifiedColumns[] = sfSocialEventPeer::USER_ADMIN;
        }

        if ($this->asfGuardUser !== null && $this->asfGuardUser->getId() !== $v) {
            $this->asfGuardUser = null;
        }


        return $this;
    } // setUserAdmin()

    /**
     * Set the value of [title] column.
     *
     * @param string $v new value
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setTitle($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->title !== $v) {
            $this->title = $v;
            $this->modifiedColumns[] = sfSocialEventPeer::TITLE;
        }


        return $this;
    } // setTitle()

    /**
     * Set the value of [description] column.
     *
     * @param string $v new value
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setDescription($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->description !== $v) {
            $this->description = $v;
            $this->modifiedColumns[] = sfSocialEventPeer::DESCRIPTION;
        }


        return $this;
    } // setDescription()

    /**
     * Set the value of [visibility] column.
     *
     * @param int $v new value
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setVisibility($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->visibility !== $v) {
            $this->visibility = $v;
            $this->modifiedColumns[] = sfSocialEventPeer::VISIBILITY;
        }


        return $this;
    } // setVisibility()

    /**
     * Set the value of [photo] column.
     *
     * @param string $v new value
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setPhoto($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->photo !== $v) {
            $this->photo = $v;
            $this->modifiedColumns[] = sfSocialEventPeer::PHOTO;
        }


        return $this;
    } // setPhoto()

    /**
     * Sets the value of [start] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setStart($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->start !== null || $dt !== null) {
            $currentDateAsString = ($this->start !== null && $tmpDt = new DateTime($this->start)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->start = $newDateAsString;
                $this->modifiedColumns[] = sfSocialEventPeer::START;
            }
        } // if either are not null


        return $this;
    } // setStart()

    /**
     * Sets the value of [end] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setEnd($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->end !== null || $dt !== null) {
            $currentDateAsString = ($this->end !== null && $tmpDt = new DateTime($this->end)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->end = $newDateAsString;
                $this->modifiedColumns[] = sfSocialEventPeer::END;
            }
        } // if either are not null


        return $this;
    } // setEnd()

    /**
     * Set the value of [location] column.
     *
     * @param string $v new value
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setLocation($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->location !== $v) {
            $this->location = $v;
            $this->modifiedColumns[] = sfSocialEventPeer::LOCATION;
        }


        return $this;
    } // setLocation()

    /**
     * Sets the value of [created_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setCreatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->created_at !== null || $dt !== null) {
            $currentDateAsString = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->created_at = $newDateAsString;
                $this->modifiedColumns[] = sfSocialEventPeer::CREATED_AT;
            }
        } // if either are not null


        return $this;
    } // setCreatedAt()

    /**
     * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function setUpdatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->updated_at !== null || $dt !== null) {
            $currentDateAsString = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->updated_at = $newDateAsString;
                $this->modifiedColumns[] = sfSocialEventPeer::UPDATED_AT;
            }
        } // if either are not null


        return $this;
    } // setUpdatedAt()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->visibility !== 0) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->user_admin = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->title = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->description = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->visibility = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->photo = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->start = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->end = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->location = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
            $this->created_at = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
            $this->updated_at = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 11; // 11 = sfSocialEventPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating sfSocialEvent object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->asfGuardUser !== null && $this->user_admin !== $this->asfGuardUser->getId()) {
            $this->asfGuardUser = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(sfSocialEventPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = sfSocialEventPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->asfGuardUser = null;
            $this->collsfSocialEventInvites = null;

            $this->collsfSocialEventUsers = null;

            $this->collEventStatuss = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(sfSocialEventPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = sfSocialEventQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasesfSocialEvent:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasesfSocialEvent:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(sfSocialEventPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasesfSocialEvent:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            // symfony_timestampable behavior
            if ($this->isModified() && !$this->isColumnModified(sfSocialEventPeer::UPDATED_AT))
            {
                $this->setUpdatedAt(time());
            }
            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
                // symfony_timestampable behavior
                if (!$this->isColumnModified(sfSocialEventPeer::CREATED_AT))
                {
                  $this->setCreatedAt(time());
                }

            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasesfSocialEvent:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                sfSocialEventPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->asfGuardUser !== null) {
                if ($this->asfGuardUser->isModified() || $this->asfGuardUser->isNew()) {
                    $affectedRows += $this->asfGuardUser->save($con);
                }
                $this->setsfGuardUser($this->asfGuardUser);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->sfSocialEventInvitesScheduledForDeletion !== null) {
                if (!$this->sfSocialEventInvitesScheduledForDeletion->isEmpty()) {
                    sfSocialEventInviteQuery::create()
                        ->filterByPrimaryKeys($this->sfSocialEventInvitesScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->sfSocialEventInvitesScheduledForDeletion = null;
                }
            }

            if ($this->collsfSocialEventInvites !== null) {
                foreach ($this->collsfSocialEventInvites as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->sfSocialEventUsersScheduledForDeletion !== null) {
                if (!$this->sfSocialEventUsersScheduledForDeletion->isEmpty()) {
                    sfSocialEventUserQuery::create()
                        ->filterByPrimaryKeys($this->sfSocialEventUsersScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->sfSocialEventUsersScheduledForDeletion = null;
                }
            }

            if ($this->collsfSocialEventUsers !== null) {
                foreach ($this->collsfSocialEventUsers as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->eventStatussScheduledForDeletion !== null) {
                if (!$this->eventStatussScheduledForDeletion->isEmpty()) {
                    EventStatusQuery::create()
                        ->filterByPrimaryKeys($this->eventStatussScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->eventStatussScheduledForDeletion = null;
                }
            }

            if ($this->collEventStatuss !== null) {
                foreach ($this->collEventStatuss as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = sfSocialEventPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . sfSocialEventPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(sfSocialEventPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::USER_ADMIN)) {
            $modifiedColumns[':p' . $index++]  = '`USER_ADMIN`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::TITLE)) {
            $modifiedColumns[':p' . $index++]  = '`TITLE`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::DESCRIPTION)) {
            $modifiedColumns[':p' . $index++]  = '`DESCRIPTION`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::VISIBILITY)) {
            $modifiedColumns[':p' . $index++]  = '`VISIBILITY`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::PHOTO)) {
            $modifiedColumns[':p' . $index++]  = '`PHOTO`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::START)) {
            $modifiedColumns[':p' . $index++]  = '`START`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::END)) {
            $modifiedColumns[':p' . $index++]  = '`END`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::LOCATION)) {
            $modifiedColumns[':p' . $index++]  = '`LOCATION`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::CREATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`CREATED_AT`';
        }
        if ($this->isColumnModified(sfSocialEventPeer::UPDATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`UPDATED_AT`';
        }

        $sql = sprintf(
            'INSERT INTO `sf_social_event` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`USER_ADMIN`':
                        $stmt->bindValue($identifier, $this->user_admin, PDO::PARAM_INT);
                        break;
                    case '`TITLE`':
                        $stmt->bindValue($identifier, $this->title, PDO::PARAM_STR);
                        break;
                    case '`DESCRIPTION`':
                        $stmt->bindValue($identifier, $this->description, PDO::PARAM_STR);
                        break;
                    case '`VISIBILITY`':
                        $stmt->bindValue($identifier, $this->visibility, PDO::PARAM_INT);
                        break;
                    case '`PHOTO`':
                        $stmt->bindValue($identifier, $this->photo, PDO::PARAM_STR);
                        break;
                    case '`START`':
                        $stmt->bindValue($identifier, $this->start, PDO::PARAM_STR);
                        break;
                    case '`END`':
                        $stmt->bindValue($identifier, $this->end, PDO::PARAM_STR);
                        break;
                    case '`LOCATION`':
                        $stmt->bindValue($identifier, $this->location, PDO::PARAM_STR);
                        break;
                    case '`CREATED_AT`':
                        $stmt->bindValue($identifier, $this->created_at, PDO::PARAM_STR);
                        break;
                    case '`UPDATED_AT`':
                        $stmt->bindValue($identifier, $this->updated_at, PDO::PARAM_STR);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->asfGuardUser !== null) {
                if (!$this->asfGuardUser->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->asfGuardUser->getValidationFailures());
                }
            }


            if (($retval = sfSocialEventPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collsfSocialEventInvites !== null) {
                    foreach ($this->collsfSocialEventInvites as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collsfSocialEventUsers !== null) {
                    foreach ($this->collsfSocialEventUsers as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collEventStatuss !== null) {
                    foreach ($this->collEventStatuss as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = sfSocialEventPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getUserAdmin();
                break;
            case 2:
                return $this->getTitle();
                break;
            case 3:
                return $this->getDescription();
                break;
            case 4:
                return $this->getVisibility();
                break;
            case 5:
                return $this->getPhoto();
                break;
            case 6:
                return $this->getStart();
                break;
            case 7:
                return $this->getEnd();
                break;
            case 8:
                return $this->getLocation();
                break;
            case 9:
                return $this->getCreatedAt();
                break;
            case 10:
                return $this->getUpdatedAt();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['sfSocialEvent'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['sfSocialEvent'][$this->getPrimaryKey()] = true;
        $keys = sfSocialEventPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getUserAdmin(),
            $keys[2] => $this->getTitle(),
            $keys[3] => $this->getDescription(),
            $keys[4] => $this->getVisibility(),
            $keys[5] => $this->getPhoto(),
            $keys[6] => $this->getStart(),
            $keys[7] => $this->getEnd(),
            $keys[8] => $this->getLocation(),
            $keys[9] => $this->getCreatedAt(),
            $keys[10] => $this->getUpdatedAt(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->asfGuardUser) {
                $result['sfGuardUser'] = $this->asfGuardUser->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collsfSocialEventInvites) {
                $result['sfSocialEventInvites'] = $this->collsfSocialEventInvites->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collsfSocialEventUsers) {
                $result['sfSocialEventUsers'] = $this->collsfSocialEventUsers->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collEventStatuss) {
                $result['EventStatuss'] = $this->collEventStatuss->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = sfSocialEventPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setUserAdmin($value);
                break;
            case 2:
                $this->setTitle($value);
                break;
            case 3:
                $this->setDescription($value);
                break;
            case 4:
                $this->setVisibility($value);
                break;
            case 5:
                $this->setPhoto($value);
                break;
            case 6:
                $this->setStart($value);
                break;
            case 7:
                $this->setEnd($value);
                break;
            case 8:
                $this->setLocation($value);
                break;
            case 9:
                $this->setCreatedAt($value);
                break;
            case 10:
                $this->setUpdatedAt($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = sfSocialEventPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setUserAdmin($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setTitle($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setDescription($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setVisibility($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setPhoto($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setStart($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setEnd($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setLocation($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setCreatedAt($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setUpdatedAt($arr[$keys[10]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(sfSocialEventPeer::DATABASE_NAME);

        if ($this->isColumnModified(sfSocialEventPeer::ID)) $criteria->add(sfSocialEventPeer::ID, $this->id);
        if ($this->isColumnModified(sfSocialEventPeer::USER_ADMIN)) $criteria->add(sfSocialEventPeer::USER_ADMIN, $this->user_admin);
        if ($this->isColumnModified(sfSocialEventPeer::TITLE)) $criteria->add(sfSocialEventPeer::TITLE, $this->title);
        if ($this->isColumnModified(sfSocialEventPeer::DESCRIPTION)) $criteria->add(sfSocialEventPeer::DESCRIPTION, $this->description);
        if ($this->isColumnModified(sfSocialEventPeer::VISIBILITY)) $criteria->add(sfSocialEventPeer::VISIBILITY, $this->visibility);
        if ($this->isColumnModified(sfSocialEventPeer::PHOTO)) $criteria->add(sfSocialEventPeer::PHOTO, $this->photo);
        if ($this->isColumnModified(sfSocialEventPeer::START)) $criteria->add(sfSocialEventPeer::START, $this->start);
        if ($this->isColumnModified(sfSocialEventPeer::END)) $criteria->add(sfSocialEventPeer::END, $this->end);
        if ($this->isColumnModified(sfSocialEventPeer::LOCATION)) $criteria->add(sfSocialEventPeer::LOCATION, $this->location);
        if ($this->isColumnModified(sfSocialEventPeer::CREATED_AT)) $criteria->add(sfSocialEventPeer::CREATED_AT, $this->created_at);
        if ($this->isColumnModified(sfSocialEventPeer::UPDATED_AT)) $criteria->add(sfSocialEventPeer::UPDATED_AT, $this->updated_at);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(sfSocialEventPeer::DATABASE_NAME);
        $criteria->add(sfSocialEventPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of sfSocialEvent (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setUserAdmin($this->getUserAdmin());
        $copyObj->setTitle($this->getTitle());
        $copyObj->setDescription($this->getDescription());
        $copyObj->setVisibility($this->getVisibility());
        $copyObj->setPhoto($this->getPhoto());
        $copyObj->setStart($this->getStart());
        $copyObj->setEnd($this->getEnd());
        $copyObj->setLocation($this->getLocation());
        $copyObj->setCreatedAt($this->getCreatedAt());
        $copyObj->setUpdatedAt($this->getUpdatedAt());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getsfSocialEventInvites() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addsfSocialEventInvite($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getsfSocialEventUsers() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addsfSocialEventUser($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getEventStatuss() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addEventStatus($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return sfSocialEvent Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return sfSocialEventPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new sfSocialEventPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a sfGuardUser object.
     *
     * @param             sfGuardUser $v
     * @return sfSocialEvent The current object (for fluent API support)
     * @throws PropelException
     */
    public function setsfGuardUser(sfGuardUser $v = null)
    {
        if ($v === null) {
            $this->setUserAdmin(NULL);
        } else {
            $this->setUserAdmin($v->getId());
        }

        $this->asfGuardUser = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the sfGuardUser object, it will not be re-added.
        if ($v !== null) {
            $v->addsfSocialEvent($this);
        }


        return $this;
    }


    /**
     * Get the associated sfGuardUser object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return sfGuardUser The associated sfGuardUser object.
     * @throws PropelException
     */
    public function getsfGuardUser(PropelPDO $con = null)
    {
        if ($this->asfGuardUser === null && ($this->user_admin !== null)) {
            $this->asfGuardUser = sfGuardUserQuery::create()->findPk($this->user_admin, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->asfGuardUser->addsfSocialEvents($this);
             */
        }

        return $this->asfGuardUser;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('sfSocialEventInvite' == $relationName) {
            $this->initsfSocialEventInvites();
        }
        if ('sfSocialEventUser' == $relationName) {
            $this->initsfSocialEventUsers();
        }
        if ('EventStatus' == $relationName) {
            $this->initEventStatuss();
        }
    }

    /**
     * Clears out the collsfSocialEventInvites collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addsfSocialEventInvites()
     */
    public function clearsfSocialEventInvites()
    {
        $this->collsfSocialEventInvites = null; // important to set this to null since that means it is uninitialized
        $this->collsfSocialEventInvitesPartial = null;
    }

    /**
     * reset is the collsfSocialEventInvites collection loaded partially
     *
     * @return void
     */
    public function resetPartialsfSocialEventInvites($v = true)
    {
        $this->collsfSocialEventInvitesPartial = $v;
    }

    /**
     * Initializes the collsfSocialEventInvites collection.
     *
     * By default this just sets the collsfSocialEventInvites collection to an empty array (like clearcollsfSocialEventInvites());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initsfSocialEventInvites($overrideExisting = true)
    {
        if (null !== $this->collsfSocialEventInvites && !$overrideExisting) {
            return;
        }
        $this->collsfSocialEventInvites = new PropelObjectCollection();
        $this->collsfSocialEventInvites->setModel('sfSocialEventInvite');
    }

    /**
     * Gets an array of sfSocialEventInvite objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this sfSocialEvent is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|sfSocialEventInvite[] List of sfSocialEventInvite objects
     * @throws PropelException
     */
    public function getsfSocialEventInvites($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialEventInvitesPartial && !$this->isNew();
        if (null === $this->collsfSocialEventInvites || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collsfSocialEventInvites) {
                // return empty collection
                $this->initsfSocialEventInvites();
            } else {
                $collsfSocialEventInvites = sfSocialEventInviteQuery::create(null, $criteria)
                    ->filterBysfSocialEvent($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collsfSocialEventInvitesPartial && count($collsfSocialEventInvites)) {
                      $this->initsfSocialEventInvites(false);

                      foreach($collsfSocialEventInvites as $obj) {
                        if (false == $this->collsfSocialEventInvites->contains($obj)) {
                          $this->collsfSocialEventInvites->append($obj);
                        }
                      }

                      $this->collsfSocialEventInvitesPartial = true;
                    }

                    return $collsfSocialEventInvites;
                }

                if($partial && $this->collsfSocialEventInvites) {
                    foreach($this->collsfSocialEventInvites as $obj) {
                        if($obj->isNew()) {
                            $collsfSocialEventInvites[] = $obj;
                        }
                    }
                }

                $this->collsfSocialEventInvites = $collsfSocialEventInvites;
                $this->collsfSocialEventInvitesPartial = false;
            }
        }

        return $this->collsfSocialEventInvites;
    }

    /**
     * Sets a collection of sfSocialEventInvite objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $sfSocialEventInvites A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setsfSocialEventInvites(PropelCollection $sfSocialEventInvites, PropelPDO $con = null)
    {
        $this->sfSocialEventInvitesScheduledForDeletion = $this->getsfSocialEventInvites(new Criteria(), $con)->diff($sfSocialEventInvites);

        foreach ($this->sfSocialEventInvitesScheduledForDeletion as $sfSocialEventInviteRemoved) {
            $sfSocialEventInviteRemoved->setsfSocialEvent(null);
        }

        $this->collsfSocialEventInvites = null;
        foreach ($sfSocialEventInvites as $sfSocialEventInvite) {
            $this->addsfSocialEventInvite($sfSocialEventInvite);
        }

        $this->collsfSocialEventInvites = $sfSocialEventInvites;
        $this->collsfSocialEventInvitesPartial = false;
    }

    /**
     * Returns the number of related sfSocialEventInvite objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related sfSocialEventInvite objects.
     * @throws PropelException
     */
    public function countsfSocialEventInvites(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialEventInvitesPartial && !$this->isNew();
        if (null === $this->collsfSocialEventInvites || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collsfSocialEventInvites) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getsfSocialEventInvites());
                }
                $query = sfSocialEventInviteQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBysfSocialEvent($this)
                    ->count($con);
            }
        } else {
            return count($this->collsfSocialEventInvites);
        }
    }

    /**
     * Method called to associate a sfSocialEventInvite object to this object
     * through the sfSocialEventInvite foreign key attribute.
     *
     * @param    sfSocialEventInvite $l sfSocialEventInvite
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function addsfSocialEventInvite(sfSocialEventInvite $l)
    {
        if ($this->collsfSocialEventInvites === null) {
            $this->initsfSocialEventInvites();
            $this->collsfSocialEventInvitesPartial = true;
        }
        if (!$this->collsfSocialEventInvites->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddsfSocialEventInvite($l);
        }

        return $this;
    }

    /**
     * @param	sfSocialEventInvite $sfSocialEventInvite The sfSocialEventInvite object to add.
     */
    protected function doAddsfSocialEventInvite($sfSocialEventInvite)
    {
        $this->collsfSocialEventInvites[]= $sfSocialEventInvite;
        $sfSocialEventInvite->setsfSocialEvent($this);
    }

    /**
     * @param	sfSocialEventInvite $sfSocialEventInvite The sfSocialEventInvite object to remove.
     */
    public function removesfSocialEventInvite($sfSocialEventInvite)
    {
        if ($this->getsfSocialEventInvites()->contains($sfSocialEventInvite)) {
            $this->collsfSocialEventInvites->remove($this->collsfSocialEventInvites->search($sfSocialEventInvite));
            if (null === $this->sfSocialEventInvitesScheduledForDeletion) {
                $this->sfSocialEventInvitesScheduledForDeletion = clone $this->collsfSocialEventInvites;
                $this->sfSocialEventInvitesScheduledForDeletion->clear();
            }
            $this->sfSocialEventInvitesScheduledForDeletion[]= $sfSocialEventInvite;
            $sfSocialEventInvite->setsfSocialEvent(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this sfSocialEvent is new, it will return
     * an empty collection; or if this sfSocialEvent has previously
     * been saved, it will retrieve related sfSocialEventInvites from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in sfSocialEvent.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|sfSocialEventInvite[] List of sfSocialEventInvite objects
     */
    public function getsfSocialEventInvitesJoinsfGuardUserRelatedByUserId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = sfSocialEventInviteQuery::create(null, $criteria);
        $query->joinWith('sfGuardUserRelatedByUserId', $join_behavior);

        return $this->getsfSocialEventInvites($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this sfSocialEvent is new, it will return
     * an empty collection; or if this sfSocialEvent has previously
     * been saved, it will retrieve related sfSocialEventInvites from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in sfSocialEvent.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|sfSocialEventInvite[] List of sfSocialEventInvite objects
     */
    public function getsfSocialEventInvitesJoinsfGuardUserRelatedByUserFrom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = sfSocialEventInviteQuery::create(null, $criteria);
        $query->joinWith('sfGuardUserRelatedByUserFrom', $join_behavior);

        return $this->getsfSocialEventInvites($query, $con);
    }

    /**
     * Clears out the collsfSocialEventUsers collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addsfSocialEventUsers()
     */
    public function clearsfSocialEventUsers()
    {
        $this->collsfSocialEventUsers = null; // important to set this to null since that means it is uninitialized
        $this->collsfSocialEventUsersPartial = null;
    }

    /**
     * reset is the collsfSocialEventUsers collection loaded partially
     *
     * @return void
     */
    public function resetPartialsfSocialEventUsers($v = true)
    {
        $this->collsfSocialEventUsersPartial = $v;
    }

    /**
     * Initializes the collsfSocialEventUsers collection.
     *
     * By default this just sets the collsfSocialEventUsers collection to an empty array (like clearcollsfSocialEventUsers());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initsfSocialEventUsers($overrideExisting = true)
    {
        if (null !== $this->collsfSocialEventUsers && !$overrideExisting) {
            return;
        }
        $this->collsfSocialEventUsers = new PropelObjectCollection();
        $this->collsfSocialEventUsers->setModel('sfSocialEventUser');
    }

    /**
     * Gets an array of sfSocialEventUser objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this sfSocialEvent is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|sfSocialEventUser[] List of sfSocialEventUser objects
     * @throws PropelException
     */
    public function getsfSocialEventUsers($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialEventUsersPartial && !$this->isNew();
        if (null === $this->collsfSocialEventUsers || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collsfSocialEventUsers) {
                // return empty collection
                $this->initsfSocialEventUsers();
            } else {
                $collsfSocialEventUsers = sfSocialEventUserQuery::create(null, $criteria)
                    ->filterBysfSocialEvent($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collsfSocialEventUsersPartial && count($collsfSocialEventUsers)) {
                      $this->initsfSocialEventUsers(false);

                      foreach($collsfSocialEventUsers as $obj) {
                        if (false == $this->collsfSocialEventUsers->contains($obj)) {
                          $this->collsfSocialEventUsers->append($obj);
                        }
                      }

                      $this->collsfSocialEventUsersPartial = true;
                    }

                    return $collsfSocialEventUsers;
                }

                if($partial && $this->collsfSocialEventUsers) {
                    foreach($this->collsfSocialEventUsers as $obj) {
                        if($obj->isNew()) {
                            $collsfSocialEventUsers[] = $obj;
                        }
                    }
                }

                $this->collsfSocialEventUsers = $collsfSocialEventUsers;
                $this->collsfSocialEventUsersPartial = false;
            }
        }

        return $this->collsfSocialEventUsers;
    }

    /**
     * Sets a collection of sfSocialEventUser objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $sfSocialEventUsers A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setsfSocialEventUsers(PropelCollection $sfSocialEventUsers, PropelPDO $con = null)
    {
        $this->sfSocialEventUsersScheduledForDeletion = $this->getsfSocialEventUsers(new Criteria(), $con)->diff($sfSocialEventUsers);

        foreach ($this->sfSocialEventUsersScheduledForDeletion as $sfSocialEventUserRemoved) {
            $sfSocialEventUserRemoved->setsfSocialEvent(null);
        }

        $this->collsfSocialEventUsers = null;
        foreach ($sfSocialEventUsers as $sfSocialEventUser) {
            $this->addsfSocialEventUser($sfSocialEventUser);
        }

        $this->collsfSocialEventUsers = $sfSocialEventUsers;
        $this->collsfSocialEventUsersPartial = false;
    }

    /**
     * Returns the number of related sfSocialEventUser objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related sfSocialEventUser objects.
     * @throws PropelException
     */
    public function countsfSocialEventUsers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collsfSocialEventUsersPartial && !$this->isNew();
        if (null === $this->collsfSocialEventUsers || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collsfSocialEventUsers) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getsfSocialEventUsers());
                }
                $query = sfSocialEventUserQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBysfSocialEvent($this)
                    ->count($con);
            }
        } else {
            return count($this->collsfSocialEventUsers);
        }
    }

    /**
     * Method called to associate a sfSocialEventUser object to this object
     * through the sfSocialEventUser foreign key attribute.
     *
     * @param    sfSocialEventUser $l sfSocialEventUser
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function addsfSocialEventUser(sfSocialEventUser $l)
    {
        if ($this->collsfSocialEventUsers === null) {
            $this->initsfSocialEventUsers();
            $this->collsfSocialEventUsersPartial = true;
        }
        if (!$this->collsfSocialEventUsers->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddsfSocialEventUser($l);
        }

        return $this;
    }

    /**
     * @param	sfSocialEventUser $sfSocialEventUser The sfSocialEventUser object to add.
     */
    protected function doAddsfSocialEventUser($sfSocialEventUser)
    {
        $this->collsfSocialEventUsers[]= $sfSocialEventUser;
        $sfSocialEventUser->setsfSocialEvent($this);
    }

    /**
     * @param	sfSocialEventUser $sfSocialEventUser The sfSocialEventUser object to remove.
     */
    public function removesfSocialEventUser($sfSocialEventUser)
    {
        if ($this->getsfSocialEventUsers()->contains($sfSocialEventUser)) {
            $this->collsfSocialEventUsers->remove($this->collsfSocialEventUsers->search($sfSocialEventUser));
            if (null === $this->sfSocialEventUsersScheduledForDeletion) {
                $this->sfSocialEventUsersScheduledForDeletion = clone $this->collsfSocialEventUsers;
                $this->sfSocialEventUsersScheduledForDeletion->clear();
            }
            $this->sfSocialEventUsersScheduledForDeletion[]= $sfSocialEventUser;
            $sfSocialEventUser->setsfSocialEvent(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this sfSocialEvent is new, it will return
     * an empty collection; or if this sfSocialEvent has previously
     * been saved, it will retrieve related sfSocialEventUsers from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in sfSocialEvent.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|sfSocialEventUser[] List of sfSocialEventUser objects
     */
    public function getsfSocialEventUsersJoinsfGuardUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = sfSocialEventUserQuery::create(null, $criteria);
        $query->joinWith('sfGuardUser', $join_behavior);

        return $this->getsfSocialEventUsers($query, $con);
    }

    /**
     * Clears out the collEventStatuss collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addEventStatuss()
     */
    public function clearEventStatuss()
    {
        $this->collEventStatuss = null; // important to set this to null since that means it is uninitialized
        $this->collEventStatussPartial = null;
    }

    /**
     * reset is the collEventStatuss collection loaded partially
     *
     * @return void
     */
    public function resetPartialEventStatuss($v = true)
    {
        $this->collEventStatussPartial = $v;
    }

    /**
     * Initializes the collEventStatuss collection.
     *
     * By default this just sets the collEventStatuss collection to an empty array (like clearcollEventStatuss());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initEventStatuss($overrideExisting = true)
    {
        if (null !== $this->collEventStatuss && !$overrideExisting) {
            return;
        }
        $this->collEventStatuss = new PropelObjectCollection();
        $this->collEventStatuss->setModel('EventStatus');
    }

    /**
     * Gets an array of EventStatus objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this sfSocialEvent is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|EventStatus[] List of EventStatus objects
     * @throws PropelException
     */
    public function getEventStatuss($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collEventStatussPartial && !$this->isNew();
        if (null === $this->collEventStatuss || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collEventStatuss) {
                // return empty collection
                $this->initEventStatuss();
            } else {
                $collEventStatuss = EventStatusQuery::create(null, $criteria)
                    ->filterBysfSocialEvent($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collEventStatussPartial && count($collEventStatuss)) {
                      $this->initEventStatuss(false);

                      foreach($collEventStatuss as $obj) {
                        if (false == $this->collEventStatuss->contains($obj)) {
                          $this->collEventStatuss->append($obj);
                        }
                      }

                      $this->collEventStatussPartial = true;
                    }

                    return $collEventStatuss;
                }

                if($partial && $this->collEventStatuss) {
                    foreach($this->collEventStatuss as $obj) {
                        if($obj->isNew()) {
                            $collEventStatuss[] = $obj;
                        }
                    }
                }

                $this->collEventStatuss = $collEventStatuss;
                $this->collEventStatussPartial = false;
            }
        }

        return $this->collEventStatuss;
    }

    /**
     * Sets a collection of EventStatus objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $eventStatuss A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setEventStatuss(PropelCollection $eventStatuss, PropelPDO $con = null)
    {
        $this->eventStatussScheduledForDeletion = $this->getEventStatuss(new Criteria(), $con)->diff($eventStatuss);

        foreach ($this->eventStatussScheduledForDeletion as $eventStatusRemoved) {
            $eventStatusRemoved->setsfSocialEvent(null);
        }

        $this->collEventStatuss = null;
        foreach ($eventStatuss as $eventStatus) {
            $this->addEventStatus($eventStatus);
        }

        $this->collEventStatuss = $eventStatuss;
        $this->collEventStatussPartial = false;
    }

    /**
     * Returns the number of related EventStatus objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related EventStatus objects.
     * @throws PropelException
     */
    public function countEventStatuss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collEventStatussPartial && !$this->isNew();
        if (null === $this->collEventStatuss || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collEventStatuss) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getEventStatuss());
                }
                $query = EventStatusQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBysfSocialEvent($this)
                    ->count($con);
            }
        } else {
            return count($this->collEventStatuss);
        }
    }

    /**
     * Method called to associate a EventStatus object to this object
     * through the EventStatus foreign key attribute.
     *
     * @param    EventStatus $l EventStatus
     * @return sfSocialEvent The current object (for fluent API support)
     */
    public function addEventStatus(EventStatus $l)
    {
        if ($this->collEventStatuss === null) {
            $this->initEventStatuss();
            $this->collEventStatussPartial = true;
        }
        if (!$this->collEventStatuss->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddEventStatus($l);
        }

        return $this;
    }

    /**
     * @param	EventStatus $eventStatus The eventStatus object to add.
     */
    protected function doAddEventStatus($eventStatus)
    {
        $this->collEventStatuss[]= $eventStatus;
        $eventStatus->setsfSocialEvent($this);
    }

    /**
     * @param	EventStatus $eventStatus The eventStatus object to remove.
     */
    public function removeEventStatus($eventStatus)
    {
        if ($this->getEventStatuss()->contains($eventStatus)) {
            $this->collEventStatuss->remove($this->collEventStatuss->search($eventStatus));
            if (null === $this->eventStatussScheduledForDeletion) {
                $this->eventStatussScheduledForDeletion = clone $this->collEventStatuss;
                $this->eventStatussScheduledForDeletion->clear();
            }
            $this->eventStatussScheduledForDeletion[]= $eventStatus;
            $eventStatus->setsfSocialEvent(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this sfSocialEvent is new, it will return
     * an empty collection; or if this sfSocialEvent has previously
     * been saved, it will retrieve related EventStatuss from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in sfSocialEvent.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|EventStatus[] List of EventStatus objects
     */
    public function getEventStatussJoinsfGuardUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = EventStatusQuery::create(null, $criteria);
        $query->joinWith('sfGuardUser', $join_behavior);

        return $this->getEventStatuss($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->user_admin = null;
        $this->title = null;
        $this->description = null;
        $this->visibility = null;
        $this->photo = null;
        $this->start = null;
        $this->end = null;
        $this->location = null;
        $this->created_at = null;
        $this->updated_at = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collsfSocialEventInvites) {
                foreach ($this->collsfSocialEventInvites as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collsfSocialEventUsers) {
                foreach ($this->collsfSocialEventUsers as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collEventStatuss) {
                foreach ($this->collEventStatuss as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collsfSocialEventInvites instanceof PropelCollection) {
            $this->collsfSocialEventInvites->clearIterator();
        }
        $this->collsfSocialEventInvites = null;
        if ($this->collsfSocialEventUsers instanceof PropelCollection) {
            $this->collsfSocialEventUsers->clearIterator();
        }
        $this->collsfSocialEventUsers = null;
        if ($this->collEventStatuss instanceof PropelCollection) {
            $this->collEventStatuss->clearIterator();
        }
        $this->collEventStatuss = null;
        $this->asfGuardUser = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(sfSocialEventPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BasesfSocialEvent:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
