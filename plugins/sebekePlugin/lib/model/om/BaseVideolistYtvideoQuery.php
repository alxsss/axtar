<?php


/**
 * Base class that represents a query for the 'videolist_ytvideo' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Fri Jan  9 05:18:53 2015
 *
 * @method VideolistYtvideoQuery orderById($order = Criteria::ASC) Order by the id column
 * @method VideolistYtvideoQuery orderByVideolistId($order = Criteria::ASC) Order by the videolist_id column
 * @method VideolistYtvideoQuery orderByYtvideoId($order = Criteria::ASC) Order by the ytvideo_id column
 * @method VideolistYtvideoQuery orderByCreatedAt($order = Criteria::ASC) Order by the created_at column
 *
 * @method VideolistYtvideoQuery groupById() Group by the id column
 * @method VideolistYtvideoQuery groupByVideolistId() Group by the videolist_id column
 * @method VideolistYtvideoQuery groupByYtvideoId() Group by the ytvideo_id column
 * @method VideolistYtvideoQuery groupByCreatedAt() Group by the created_at column
 *
 * @method VideolistYtvideoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method VideolistYtvideoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method VideolistYtvideoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method VideolistYtvideoQuery leftJoinVideolist($relationAlias = null) Adds a LEFT JOIN clause to the query using the Videolist relation
 * @method VideolistYtvideoQuery rightJoinVideolist($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Videolist relation
 * @method VideolistYtvideoQuery innerJoinVideolist($relationAlias = null) Adds a INNER JOIN clause to the query using the Videolist relation
 *
 * @method VideolistYtvideo findOne(PropelPDO $con = null) Return the first VideolistYtvideo matching the query
 * @method VideolistYtvideo findOneOrCreate(PropelPDO $con = null) Return the first VideolistYtvideo matching the query, or a new VideolistYtvideo object populated from the query conditions when no match is found
 *
 * @method VideolistYtvideo findOneById(int $id) Return the first VideolistYtvideo filtered by the id column
 * @method VideolistYtvideo findOneByVideolistId(int $videolist_id) Return the first VideolistYtvideo filtered by the videolist_id column
 * @method VideolistYtvideo findOneByYtvideoId(string $ytvideo_id) Return the first VideolistYtvideo filtered by the ytvideo_id column
 * @method VideolistYtvideo findOneByCreatedAt(string $created_at) Return the first VideolistYtvideo filtered by the created_at column
 *
 * @method array findById(int $id) Return VideolistYtvideo objects filtered by the id column
 * @method array findByVideolistId(int $videolist_id) Return VideolistYtvideo objects filtered by the videolist_id column
 * @method array findByYtvideoId(string $ytvideo_id) Return VideolistYtvideo objects filtered by the ytvideo_id column
 * @method array findByCreatedAt(string $created_at) Return VideolistYtvideo objects filtered by the created_at column
 *
 * @package    propel.generator.plugins.sebekePlugin.lib.model.om
 */
abstract class BaseVideolistYtvideoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseVideolistYtvideoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'VideolistYtvideo', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new VideolistYtvideoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     VideolistYtvideoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return VideolistYtvideoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof VideolistYtvideoQuery) {
            return $criteria;
        }
        $query = new VideolistYtvideoQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   VideolistYtvideo|VideolistYtvideo[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = VideolistYtvideoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(VideolistYtvideoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   VideolistYtvideo A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `VIDEOLIST_ID`, `YTVIDEO_ID`, `CREATED_AT` FROM `videolist_ytvideo` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new VideolistYtvideo();
            $obj->hydrate($row);
            VideolistYtvideoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return VideolistYtvideo|VideolistYtvideo[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|VideolistYtvideo[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(VideolistYtvideoPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(VideolistYtvideoPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(VideolistYtvideoPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the videolist_id column
     *
     * Example usage:
     * <code>
     * $query->filterByVideolistId(1234); // WHERE videolist_id = 1234
     * $query->filterByVideolistId(array(12, 34)); // WHERE videolist_id IN (12, 34)
     * $query->filterByVideolistId(array('min' => 12)); // WHERE videolist_id > 12
     * </code>
     *
     * @see       filterByVideolist()
     *
     * @param     mixed $videolistId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function filterByVideolistId($videolistId = null, $comparison = null)
    {
        if (is_array($videolistId)) {
            $useMinMax = false;
            if (isset($videolistId['min'])) {
                $this->addUsingAlias(VideolistYtvideoPeer::VIDEOLIST_ID, $videolistId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($videolistId['max'])) {
                $this->addUsingAlias(VideolistYtvideoPeer::VIDEOLIST_ID, $videolistId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VideolistYtvideoPeer::VIDEOLIST_ID, $videolistId, $comparison);
    }

    /**
     * Filter the query on the ytvideo_id column
     *
     * Example usage:
     * <code>
     * $query->filterByYtvideoId('fooValue');   // WHERE ytvideo_id = 'fooValue'
     * $query->filterByYtvideoId('%fooValue%'); // WHERE ytvideo_id LIKE '%fooValue%'
     * </code>
     *
     * @param     string $ytvideoId The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function filterByYtvideoId($ytvideoId = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($ytvideoId)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $ytvideoId)) {
                $ytvideoId = str_replace('*', '%', $ytvideoId);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(VideolistYtvideoPeer::YTVIDEO_ID, $ytvideoId, $comparison);
    }

    /**
     * Filter the query on the created_at column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedAt('2011-03-14'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt('now'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt(array('max' => 'yesterday')); // WHERE created_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $createdAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function filterByCreatedAt($createdAt = null, $comparison = null)
    {
        if (is_array($createdAt)) {
            $useMinMax = false;
            if (isset($createdAt['min'])) {
                $this->addUsingAlias(VideolistYtvideoPeer::CREATED_AT, $createdAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createdAt['max'])) {
                $this->addUsingAlias(VideolistYtvideoPeer::CREATED_AT, $createdAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VideolistYtvideoPeer::CREATED_AT, $createdAt, $comparison);
    }

    /**
     * Filter the query by a related Videolist object
     *
     * @param   Videolist|PropelObjectCollection $videolist The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VideolistYtvideoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVideolist($videolist, $comparison = null)
    {
        if ($videolist instanceof Videolist) {
            return $this
                ->addUsingAlias(VideolistYtvideoPeer::VIDEOLIST_ID, $videolist->getId(), $comparison);
        } elseif ($videolist instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(VideolistYtvideoPeer::VIDEOLIST_ID, $videolist->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByVideolist() only accepts arguments of type Videolist or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Videolist relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function joinVideolist($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Videolist');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Videolist');
        }

        return $this;
    }

    /**
     * Use the Videolist relation Videolist object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VideolistQuery A secondary query class using the current class as primary query
     */
    public function useVideolistQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinVideolist($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Videolist', 'VideolistQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   VideolistYtvideo $videolistYtvideo Object to remove from the list of results
     *
     * @return VideolistYtvideoQuery The current query, for fluid interface
     */
    public function prune($videolistYtvideo = null)
    {
        if ($videolistYtvideo) {
            $this->addUsingAlias(VideolistYtvideoPeer::ID, $videolistYtvideo->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
