<?php


/**
 * Base class that represents a query for the 'updates' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Fri Jan  9 03:26:40 2015
 *
 * @method UpdatesQuery orderByPid($order = Criteria::ASC) Order by the pid column
 * @method UpdatesQuery orderById($order = Criteria::ASC) Order by the id column
 * @method UpdatesQuery orderByUserId($order = Criteria::ASC) Order by the user_id column
 * @method UpdatesQuery orderByFriendId($order = Criteria::ASC) Order by the friend_id column
 * @method UpdatesQuery orderByGroupId($order = Criteria::ASC) Order by the group_id column
 * @method UpdatesQuery orderByFStatusName($order = Criteria::ASC) Order by the f_status_name column
 * @method UpdatesQuery orderByPOwnerId($order = Criteria::ASC) Order by the p_owner_id column
 * @method UpdatesQuery orderByNumComment($order = Criteria::ASC) Order by the num_comment column
 * @method UpdatesQuery orderByDescription($order = Criteria::ASC) Order by the description column
 * @method UpdatesQuery orderByCreatedAt($order = Criteria::ASC) Order by the created_at column
 *
 * @method UpdatesQuery groupByPid() Group by the pid column
 * @method UpdatesQuery groupById() Group by the id column
 * @method UpdatesQuery groupByUserId() Group by the user_id column
 * @method UpdatesQuery groupByFriendId() Group by the friend_id column
 * @method UpdatesQuery groupByGroupId() Group by the group_id column
 * @method UpdatesQuery groupByFStatusName() Group by the f_status_name column
 * @method UpdatesQuery groupByPOwnerId() Group by the p_owner_id column
 * @method UpdatesQuery groupByNumComment() Group by the num_comment column
 * @method UpdatesQuery groupByDescription() Group by the description column
 * @method UpdatesQuery groupByCreatedAt() Group by the created_at column
 *
 * @method UpdatesQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method UpdatesQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method UpdatesQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method UpdatesQuery leftJoinsfGuardUser($relationAlias = null) Adds a LEFT JOIN clause to the query using the sfGuardUser relation
 * @method UpdatesQuery rightJoinsfGuardUser($relationAlias = null) Adds a RIGHT JOIN clause to the query using the sfGuardUser relation
 * @method UpdatesQuery innerJoinsfGuardUser($relationAlias = null) Adds a INNER JOIN clause to the query using the sfGuardUser relation
 *
 * @method Updates findOne(PropelPDO $con = null) Return the first Updates matching the query
 * @method Updates findOneOrCreate(PropelPDO $con = null) Return the first Updates matching the query, or a new Updates object populated from the query conditions when no match is found
 *
 * @method Updates findOneByPid(int $pid) Return the first Updates filtered by the pid column
 * @method Updates findOneById(int $id) Return the first Updates filtered by the id column
 * @method Updates findOneByUserId(int $user_id) Return the first Updates filtered by the user_id column
 * @method Updates findOneByFriendId(string $friend_id) Return the first Updates filtered by the friend_id column
 * @method Updates findOneByGroupId(string $group_id) Return the first Updates filtered by the group_id column
 * @method Updates findOneByFStatusName(string $f_status_name) Return the first Updates filtered by the f_status_name column
 * @method Updates findOneByPOwnerId(string $p_owner_id) Return the first Updates filtered by the p_owner_id column
 * @method Updates findOneByNumComment(string $num_comment) Return the first Updates filtered by the num_comment column
 * @method Updates findOneByDescription(string $description) Return the first Updates filtered by the description column
 * @method Updates findOneByCreatedAt(string $created_at) Return the first Updates filtered by the created_at column
 *
 * @method array findByPid(int $pid) Return Updates objects filtered by the pid column
 * @method array findById(int $id) Return Updates objects filtered by the id column
 * @method array findByUserId(int $user_id) Return Updates objects filtered by the user_id column
 * @method array findByFriendId(string $friend_id) Return Updates objects filtered by the friend_id column
 * @method array findByGroupId(string $group_id) Return Updates objects filtered by the group_id column
 * @method array findByFStatusName(string $f_status_name) Return Updates objects filtered by the f_status_name column
 * @method array findByPOwnerId(string $p_owner_id) Return Updates objects filtered by the p_owner_id column
 * @method array findByNumComment(string $num_comment) Return Updates objects filtered by the num_comment column
 * @method array findByDescription(string $description) Return Updates objects filtered by the description column
 * @method array findByCreatedAt(string $created_at) Return Updates objects filtered by the created_at column
 *
 * @package    propel.generator.plugins.sebekePlugin.lib.model.om
 */
abstract class BaseUpdatesQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseUpdatesQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Updates', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new UpdatesQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     UpdatesQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return UpdatesQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof UpdatesQuery) {
            return $criteria;
        }
        $query = new UpdatesQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Updates|Updates[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = UpdatesPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(UpdatesPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Updates A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `PID`, `ID`, `USER_ID`, `FRIEND_ID`, `GROUP_ID`, `F_STATUS_NAME`, `P_OWNER_ID`, `NUM_COMMENT`, `DESCRIPTION`, `CREATED_AT` FROM `updates` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Updates();
            $obj->hydrate($row);
            UpdatesPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Updates|Updates[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Updates[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(UpdatesPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(UpdatesPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the pid column
     *
     * Example usage:
     * <code>
     * $query->filterByPid(1234); // WHERE pid = 1234
     * $query->filterByPid(array(12, 34)); // WHERE pid IN (12, 34)
     * $query->filterByPid(array('min' => 12)); // WHERE pid > 12
     * </code>
     *
     * @param     mixed $pid The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByPid($pid = null, $comparison = null)
    {
        if (is_array($pid)) {
            $useMinMax = false;
            if (isset($pid['min'])) {
                $this->addUsingAlias(UpdatesPeer::PID, $pid['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($pid['max'])) {
                $this->addUsingAlias(UpdatesPeer::PID, $pid['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::PID, $pid, $comparison);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(UpdatesPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the user_id column
     *
     * Example usage:
     * <code>
     * $query->filterByUserId(1234); // WHERE user_id = 1234
     * $query->filterByUserId(array(12, 34)); // WHERE user_id IN (12, 34)
     * $query->filterByUserId(array('min' => 12)); // WHERE user_id > 12
     * </code>
     *
     * @see       filterBysfGuardUser()
     *
     * @param     mixed $userId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByUserId($userId = null, $comparison = null)
    {
        if (is_array($userId)) {
            $useMinMax = false;
            if (isset($userId['min'])) {
                $this->addUsingAlias(UpdatesPeer::USER_ID, $userId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($userId['max'])) {
                $this->addUsingAlias(UpdatesPeer::USER_ID, $userId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::USER_ID, $userId, $comparison);
    }

    /**
     * Filter the query on the friend_id column
     *
     * Example usage:
     * <code>
     * $query->filterByFriendId('fooValue');   // WHERE friend_id = 'fooValue'
     * $query->filterByFriendId('%fooValue%'); // WHERE friend_id LIKE '%fooValue%'
     * </code>
     *
     * @param     string $friendId The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByFriendId($friendId = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($friendId)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $friendId)) {
                $friendId = str_replace('*', '%', $friendId);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::FRIEND_ID, $friendId, $comparison);
    }

    /**
     * Filter the query on the group_id column
     *
     * Example usage:
     * <code>
     * $query->filterByGroupId('fooValue');   // WHERE group_id = 'fooValue'
     * $query->filterByGroupId('%fooValue%'); // WHERE group_id LIKE '%fooValue%'
     * </code>
     *
     * @param     string $groupId The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByGroupId($groupId = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($groupId)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $groupId)) {
                $groupId = str_replace('*', '%', $groupId);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::GROUP_ID, $groupId, $comparison);
    }

    /**
     * Filter the query on the f_status_name column
     *
     * Example usage:
     * <code>
     * $query->filterByFStatusName('fooValue');   // WHERE f_status_name = 'fooValue'
     * $query->filterByFStatusName('%fooValue%'); // WHERE f_status_name LIKE '%fooValue%'
     * </code>
     *
     * @param     string $fStatusName The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByFStatusName($fStatusName = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($fStatusName)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $fStatusName)) {
                $fStatusName = str_replace('*', '%', $fStatusName);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::F_STATUS_NAME, $fStatusName, $comparison);
    }

    /**
     * Filter the query on the p_owner_id column
     *
     * Example usage:
     * <code>
     * $query->filterByPOwnerId('fooValue');   // WHERE p_owner_id = 'fooValue'
     * $query->filterByPOwnerId('%fooValue%'); // WHERE p_owner_id LIKE '%fooValue%'
     * </code>
     *
     * @param     string $pOwnerId The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByPOwnerId($pOwnerId = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($pOwnerId)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $pOwnerId)) {
                $pOwnerId = str_replace('*', '%', $pOwnerId);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::P_OWNER_ID, $pOwnerId, $comparison);
    }

    /**
     * Filter the query on the num_comment column
     *
     * Example usage:
     * <code>
     * $query->filterByNumComment('fooValue');   // WHERE num_comment = 'fooValue'
     * $query->filterByNumComment('%fooValue%'); // WHERE num_comment LIKE '%fooValue%'
     * </code>
     *
     * @param     string $numComment The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByNumComment($numComment = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($numComment)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $numComment)) {
                $numComment = str_replace('*', '%', $numComment);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::NUM_COMMENT, $numComment, $comparison);
    }

    /**
     * Filter the query on the description column
     *
     * Example usage:
     * <code>
     * $query->filterByDescription('fooValue');   // WHERE description = 'fooValue'
     * $query->filterByDescription('%fooValue%'); // WHERE description LIKE '%fooValue%'
     * </code>
     *
     * @param     string $description The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByDescription($description = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($description)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $description)) {
                $description = str_replace('*', '%', $description);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::DESCRIPTION, $description, $comparison);
    }

    /**
     * Filter the query on the created_at column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedAt('2011-03-14'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt('now'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt(array('max' => 'yesterday')); // WHERE created_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $createdAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function filterByCreatedAt($createdAt = null, $comparison = null)
    {
        if (is_array($createdAt)) {
            $useMinMax = false;
            if (isset($createdAt['min'])) {
                $this->addUsingAlias(UpdatesPeer::CREATED_AT, $createdAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createdAt['max'])) {
                $this->addUsingAlias(UpdatesPeer::CREATED_AT, $createdAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(UpdatesPeer::CREATED_AT, $createdAt, $comparison);
    }

    /**
     * Filter the query by a related sfGuardUser object
     *
     * @param   sfGuardUser|PropelObjectCollection $sfGuardUser The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UpdatesQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBysfGuardUser($sfGuardUser, $comparison = null)
    {
        if ($sfGuardUser instanceof sfGuardUser) {
            return $this
                ->addUsingAlias(UpdatesPeer::USER_ID, $sfGuardUser->getId(), $comparison);
        } elseif ($sfGuardUser instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(UpdatesPeer::USER_ID, $sfGuardUser->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterBysfGuardUser() only accepts arguments of type sfGuardUser or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the sfGuardUser relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function joinsfGuardUser($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('sfGuardUser');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'sfGuardUser');
        }

        return $this;
    }

    /**
     * Use the sfGuardUser relation sfGuardUser object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   sfGuardUserQuery A secondary query class using the current class as primary query
     */
    public function usesfGuardUserQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinsfGuardUser($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'sfGuardUser', 'sfGuardUserQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Updates $updates Object to remove from the list of results
     *
     * @return UpdatesQuery The current query, for fluid interface
     */
    public function prune($updates = null)
    {
        if ($updates) {
            $this->addUsingAlias(UpdatesPeer::ID, $updates->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
